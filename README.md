# args

```shell
# long flag with `=`
$ app --ip=127.0.0.1

# long flag without `=`
$ app --ip 127.0.0.1

# short flag with `=`
$ app -i=127.0.0.1

# short flag without `=`
$ app -i 127.0.0.1

# boolean flag with value
$ app --debug=true

# boolean flag without value
$ app --debug
```
